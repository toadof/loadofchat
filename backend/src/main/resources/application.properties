# Server port — Render injects PORT; default 8080 for local dev
server.port=${PORT:8080}

# Comma-separated allowed origins for CORS and WebSocket (override in production)
app.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5173}

# Room cleanup: remove empty rooms after 10 minutes
app.room-cleanup-minutes=10
